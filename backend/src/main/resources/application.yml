spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/vibe
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: ViBe-GrOuPwArE-S3cr3t-K3y-F0r-JWT-T0k3n-Auth3ntication-2024-Main
  expiration: 86400000

server:
  port: 8080
